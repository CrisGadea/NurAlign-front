/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SesionChartComponent } from './sesion-chart.component';

describe('SesionChartComponent', () => {
  let component: SesionChartComponent;
  let fixture: ComponentFixture<SesionChartComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [SesionChartComponent]
    });
    fixture = TestBed.createComponent(SesionChartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/